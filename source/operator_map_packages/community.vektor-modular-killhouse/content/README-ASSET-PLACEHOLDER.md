# Package content placeholders

- `[DEPENDENCY ASSETBUNDLE]` -> `operator_vektor_killhouse`
- `[SCENE ASSETBUNDLE]` -> `operator_vektor_killhouse_scenes`

The declared filenames above are not present in Git. Build them from authorized
local inputs; do not rename this README to a payload filename.
